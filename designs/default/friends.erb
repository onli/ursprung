<%# -*- encoding : utf-8 -*- %>
<% if friends.length > 0 %>
    <h3>Friends</h3>
    <ul id="friendList">
        <% friends.each do |friend| %>
            <li><a href="<%= friend.url %>"><%= friend.name %></a></li>
        <% end %>
    </ul>
<% end %>
<% if ! isAdmin? %>
    <br %>
    <a href="<%= friendManagerUrl %>addFriend?id=<%= blogOwnerMail %>&amp;url=<%=u 'http://' + request.host_with_port %>">add <%= blogOwner %> as Friend!</a>
<% end %>